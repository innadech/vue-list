let newFlower = 'lilies'

let flowers = ['roses', 'violets', 'blooms', 'tulips']

let mappedFlowers = flowers.map(f => (f === 'violets' ? newFlower : f))

mappedFlowers

// let mappedFlowers = flowers.map(f => f[0].toUpperCase())
// mappedFlowers
// flowers
// & - амперсанд
// $ - знак доллараа
